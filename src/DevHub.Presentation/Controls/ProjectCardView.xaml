<UserControl x:Class="DevHub.Presentation.Views.ProjectCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Border Background="White"
            CornerRadius="8"
            Padding="16"
            Margin="8"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            Width="280"
            Cursor="Hand">
        <StackPanel>
            <TextBlock Text="{Binding Name}"
                       FontSize="16"
                       FontWeight="SemiBold" />
            <TextBlock Text="{Binding Description}"
                       Foreground="#666"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       MaxHeight="40" />
            <TextBlock Text="{Binding FormattedUpdatedAt}"
                       Foreground="#999"
                       FontSize="11"
                       Margin="0,8,0,0" />
        </StackPanel>
    </Border>
</UserControl>
