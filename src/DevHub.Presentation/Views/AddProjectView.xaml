<Window x:Class="DevHub.Presentation.Views.AddProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="{Binding Title}"
        Width="{Binding Width}"
        Height="{Binding Height}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Name -->
        <TextBlock Grid.Row="0" Text="Name" FontWeight="SemiBold" Margin="0,0,0,4" />
        <hc:TextBox Grid.Row="0"
                 Margin="0,0,0,16"
                 hc:InfoElement.Placeholder="Project name"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <!-- Path -->
        <TextBlock Grid.Row="1" Text="Path" FontWeight="SemiBold" Margin="0,0,0,4" />
        <DockPanel Grid.Row="1" Margin="0,0,0,16">
            <Button DockPanel.Dock="Right"
                    Content="Browse..."
                    Command="{Binding BrowsePathCommand}"
                    Margin="8,0,0,0" />
            <hc:TextBox Text="{Binding Path, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     hc:InfoElement.Placeholder="Project directory path" />
        </DockPanel>

        <!-- Language -->
        <TextBlock Grid.Row="2" Text="Language" FontWeight="SemiBold" Margin="0,0,0,4" />
        <ComboBox Grid.Row="2"
                  ItemsSource="{Binding Languages}"
                  SelectedItem="{Binding Language}"
                  Margin="0,0,0,16" />

        <!-- Description -->
        <TextBlock Grid.Row="3" Text="Description" FontWeight="SemiBold" Margin="0,0,0,4" />
        <hc:TextBox Grid.Row="3"
                 Text="{Binding Description}"
                 hc:InfoElement.Placeholder="Optional description"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="80"
                 Margin="0,0,0,16" />

        <!-- Error -->
        <TextBlock Grid.Row="4"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                   VerticalAlignment="Bottom" />

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Margin="0,0,8,0"
                    Padding="16,0" />
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Padding="16,0"
                    Style="{StaticResource ButtonPrimary}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}" />
        </StackPanel>
    </Grid>
</Window>
