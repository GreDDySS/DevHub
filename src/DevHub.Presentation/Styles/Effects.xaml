<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ==================== EFFECTS ==================== -->
    <!-- Shadows -->
    <DropShadowEffect x:Key="ShadowSm" BlurRadius="6" ShadowDepth="1" Opacity="0.15" Direction="270" Color="#000000" />
    <DropShadowEffect x:Key="ShadowMd" BlurRadius="10" ShadowDepth="2" Opacity="0.2" Direction="270" Color="#000000" />
    <DropShadowEffect x:Key="ShadowLg" BlurRadius="16" ShadowDepth="4" Opacity="0.25" Direction="270" Color="#000000" />
    <DropShadowEffect x:Key="ShadowXl" BlurRadius="24" ShadowDepth="6" Opacity="0.3" Direction="270" Color="#000000" />
    <DropShadowEffect x:Key="ShadowGlow" BlurRadius="16" ShadowDepth="0" Opacity="0.25" Color="#337A2C" />

    <!-- ==================== CORNER RADII ==================== -->
    <CornerRadius x:Key="RadiusSm">6</CornerRadius>
    <CornerRadius x:Key="RadiusMd">12</CornerRadius>
    <CornerRadius x:Key="RadiusLg">16</CornerRadius>
    <CornerRadius x:Key="RadiusXl">20</CornerRadius>
    <CornerRadius x:Key="RadiusFull">9999</CornerRadius>

    <!-- ==================== SPACING ==================== -->
    <Thickness x:Key="SpacingXs">2</Thickness>
    <Thickness x:Key="SpacingSm">4</Thickness>
    <Thickness x:Key="SpacingMd">8</Thickness>
    <Thickness x:Key="SpacingLg">12</Thickness>
    <Thickness x:Key="SpacingXl">16</Thickness>
    <Thickness x:Key="Spacing2xl">20</Thickness>
    <Thickness x:Key="Spacing3xl">24</Thickness>
    <Thickness x:Key="Spacing4xl">32</Thickness>

    <!-- ==================== TYPOGRAPHY ==================== -->
    <Style x:Key="TextDisplay" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>
    <Style x:Key="TextH1" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>
    <Style x:Key="TextH2" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>
    <Style x:Key="TextH3" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>
    <Style x:Key="TextBody" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    </Style>
    <Style x:Key="TextSmall" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
    </Style>
    <Style x:Key="TextTiny" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
    </Style>

</ResourceDictionary>
